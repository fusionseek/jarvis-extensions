# 字数统计（脚手架）

粘一段文字，实时数出字符、单词与行数；一键复制摘要。

- **授权**：不需要任何授权。
- **设置**：无。
- **Inbox**：无。
- **存储**：把上次输入的文本记在 `storage`，重新进入时恢复。

把这个目录拷到 `extensions/<你的-id>/`，改 `extension.json` 与 `src/index.ts`，
然后按 [开发手册](../../docs/12-developer-guide.md) 走。
